[gd_scene load_steps=5 format=2]

[ext_resource path="res://Scripts/CameraFocus.gd" type="Script" id=1]
[ext_resource path="res://Scripts/CameraTarget.gd" type="Script" id=2]
[ext_resource path="res://Scripts/CameraController.gd" type="Script" id=3]

[sub_resource type="GDScript" id=20]
script/source = "extends Camera


# Declare member variables here. Examples:
# var a = 2
# var b = \"text\"
var mouse_sens = 0.1
var camera_anglev=0

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass

"

[node name="CameraController" type="Spatial"]
script = ExtResource( 3 )

[node name="CameraFocus" type="Spatial" parent="."]
script = ExtResource( 1 )

[node name="CameraTarget" type="Spatial" parent="CameraFocus"]
script = ExtResource( 2 )

[node name="Main Camera" type="Camera" parent="."]
cull_mask = 1023
current = true
script = SubResource( 20 )
